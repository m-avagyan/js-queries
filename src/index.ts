import find from './functions/find';
import filter from './functions/filter';
import unique from './functions/unique';
import sort from './functions/sort';

const jsq = {
  find,
  filter,
  unique,
  sort,
};

export { find, filter, unique, sort };

export default jsq;
